# Optional endpoint for the live job-description reader.
# The endpoint should accept { "description": "..." } and return:
# { "role": "...", "detail": "...", "signals": ["..."], "note": "..." }
VITE_AI_ENDPOINT=
